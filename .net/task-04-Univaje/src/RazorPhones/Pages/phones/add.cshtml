@page
@model RazorPhones.Pages.phones.CreateModel

@{
    ViewData["Title"] = "Create";
    /*
     * 3. Add page (/phones/add) that contains fields for all Phone class' properties except the fields that are populated by the app
        (the fields are marked with code comments on the class declaration). Use `Phone` class as the bind property type and use
         `Phone` as the property's name. Model the fields so that POSTing the form will add a new phone information to the database.
          The page must not contain any other fields for data input. Use `button` element as the submit button.
          When the POST is successful the app redirects to /phones page to list all the phones including the newly added phone.
    */
}

<h1>Create</h1>

<h4>Phone</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Phone.Make" class="control-label"></label>
                <input asp-for="Phone.Make" class="form-control" />
                <span asp-validation-for="Phone.Make" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.Model" class="control-label"></label>
                <input asp-for="Phone.Model" class="form-control" />
                <span asp-validation-for="Phone.Model" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.RAM" class="control-label"></label>
                <input asp-for="Phone.RAM" class="form-control" />
                <span asp-validation-for="Phone.RAM" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.PublishDate" class="control-label"></label>
                <input asp-for="Phone.PublishDate" class="form-control" />
                <span asp-validation-for="Phone.PublishDate" class="text-danger"></span>
            </div>
           <!-- <div class="form-group">
                <label asp-for="Phone.Created" class="control-label"></label>
                <input asp-for="Phone.Created" class="form-control" />
                <span asp-validation-for="Phone.Created" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.Modified" class="control-label"></label>
                <input asp-for="Phone.Modified" class="form-control" />
                <span asp-validation-for="Phone.Modified" class="text-danger"></span>
             </div>-->
            <div class="form-group">
                <button type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
